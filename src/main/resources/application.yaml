spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nickdb
    username: nickkru
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
